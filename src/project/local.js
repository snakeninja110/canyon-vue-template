//建议所有缓存localStorage写在这里
// import { getItem, setItem } from "koi/util/storage";

// import { create as createDate, format as formatDate, addDay } from "koi/util/date";

// eslint-disable-next-line
export default function (ajax, env, model) {

    // 存储集合
    let local = {};


    return local;
}